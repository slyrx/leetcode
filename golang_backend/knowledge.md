```golang
+-------------------+
|   Pointer (指针)   | --> 指向底层数组的起始位置
+-------------------+
|    Length (长度)    | --> 切片中当前存储的元素数量
+-------------------+
|   Capacity (容量)   | --> 切片从起始位置到底层数组末尾的可用元素数量
+-------------------+

```
